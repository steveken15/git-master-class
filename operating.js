 //the &&(and) operators
 
 let age = 22;
    let  isAdult = true;
if(age>=18 && isAdult) {
    console.log("Elligible for the party");
} else {
    console.log("Not Elligible for the party")
} 
console.log("Party start at 9pm");
     //the OR operators
const isAdmin = false;
const isGuestuser = true;
if(isAdmin || isGuestuser) {
    console.log("Access granted");
} else {
    console.log("Access denied");
}
console.log("Welcome to steveken contribution group");
       //the Not operators
let hasnotpaid = false;
if (!hasnotpaid) {
    console.log("can come to class");
}  else {
    console.log("cannot come to class");}

    let isWell = false;
    if (!isWell) {
        console.log("can come to school");   // the ! operator is used to reverse the value of the variable, so if isWell is false, !isWell will be true, allowing the person to come to school. If isWell were true, !isWell would be false, and the person would not be able to come to school.
    }  else {
        console.log("cannot come to school")
    }
   
      //Arithmetic operators
      let total = 8 + 6;  //addition
      console.log(total);

      let balance = 48 - 12;  //subtraction
      console.log(balance);

let gadgetprice = 16 * 4;   //multiplication
console.log(gadgetprice);

let goldcost = 100 / 4;  //division
console.log(goldcost);


let remainder = 20 % 7; //modulus (remainder)
console.log(remainder);


let power = 3 ** 3; //exponentiation (powers)
console.log(power);
 
  //increment count
  